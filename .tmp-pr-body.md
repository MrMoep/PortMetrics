## Summary
- FIFO-Engine: BUY→Lots, SELL verbraucht älteste Lots zuerst
- Full rebuild aus Activities (`POST /api/fifo/rebuild`)
- Offene Lots inkl. unrealisiertem Gewinn (`GET /api/lots`)
- Verkaufs-Simulator mit Steuer-Schätzung (`POST /api/simulate/sell`)
- Sync Ghostfolio baut Lots danach neu auf
- CLI: `portmetrics-worker rebuild-fifo`

Closes #2

## Test plan
- [x] Unit-Tests FIFO-Reihenfolge / Tax
- [ ] CI mit Postgres: Rebuild + Simulate Integration
- [ ] `ruff` + Coverage ≥70% in CI
